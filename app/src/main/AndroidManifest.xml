<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".abcp.OrdersActivity" android:exported="false" />
        <activity android:name=".MainShellActivity" android:exported="false" />
        <activity android:name=".abcp.OrderDetailsActivity" android:exported="false" />
        <activity android:name=".SearchActivity" android:exported="false" />
        <activity android:name=".abcp.OffersActivity" android:exported="false" />
        <activity android:name=".abcp.CartActivity" android:exported="false" />
        <activity android:name=".abcp.CheckoutActivity" android:exported="false" />
        <activity android:name=".abcp.NotificationsActivity" android:exported="false" />
        <activity android:name=".NewsActivity" android:exported="false" />
        <activity android:name=".ChatActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".VinSearchActivity" />
        <activity android:name=".GarageActivity" />

        <!-- Laximo -->
        <activity android:name=".laximo.VehicleActivity" />
        <activity android:name=".laximo.QuickGroupsActivity" />
        <activity android:name=".laximo.CatalogCategoriesActivity" />
        <activity android:name=".laximo.CatalogUnitsActivity" />
        <activity android:name=".laximo.UnitDetailsActivity" />

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>